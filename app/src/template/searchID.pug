doctype html
html
  head
    meta(charset='utf-8')
    meta(name='viewport' content='width=device-width, initial-scale=1')
    link(rel='stylesheet' href=require('bootstrap/dist/css/bootstrap.min.css'))
  body
    div#vue-app
      form.search.font12
        div.form-group
          label(for='url') 直播间地址：
          input.form-control.font12#url(type='text' v-model='inputUrl')
        div.form-group.search-mg
          label(for='id') 直播间ID：
          div.input-group
            input.form-control.search-id.font12#id(type='text' v-model='id' readonly)
            button.btn.input-group-addon.search-copy.cur-p(type='button' title='复制' v-on:click='onCopy()')
              i.icon.icon-clipboard
        button.btn.btn-block.btn-lg.btn-primary.font12.cur-p(type='button' title='获取直播间ID' v-on:click='onGetRoomId()')
          i.icon.icon-search.mr
          | 获取直播间ID
      // 弹出层
      transition(name='fade')
        div.alert.alert-warning.text-center.alert-body.font12(role='alert' v-if='warnUrl') 直播间地址错误！
      transition(name='fade')
        div.alert.alert-danger.text-center.alert-body.font12(role='alert' v-if='error != null')
          | 请求错误，错误代码：
          b {{ error }}
          | 。
    script(type='text/javascript' src=require('../../.dll/dll'))